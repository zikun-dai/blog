确认使用 Astro 作为框架；

确认使用子文件夹结构（`astro-blog/`）

构建目标：GitHub page；

提示语法选择Markdown 指令。

主题：

1. 我希望简洁优雅一些，白色的主题色，有按键可以切换深色/浅色；
2. 之前hexo的主题和ui我非常不满意，你不太需要参考之前的设置，把post相关的东西移植过去即可，主题和ui设置不需要参考；

一些功能构想：

1. 有的文章中我添加了tag，我希望有一块专门的区域显示所有tag，点击tag就可以显示带这个tag的所有post；


<!-- 目前的代码框架是基于hexo的，但hexo的界面过于简陋，且对于markdown语法的支持没有跟进（比如不支持警示框）

我希望使用更先进的代码框架来重构我的blog，我打算使用astro，如果你有更好的推荐，也请告诉我；

我认为直接在当前仓库进行重构比较复杂，或许应该新建一个子文件夹来构建？

我已经在本地通过brew安装了node.js和npm，允许你使用终端进行安装新的包和库。

先帮我在当前文件夹下生成一个详细的构建说明、技术路线，我审查并根据自己的想法修改之后，再帮我构建完整的新blog框架 -->